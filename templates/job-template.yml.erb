- name: <%= branch_name %>
  plan:
  - get: git-branch
    resource: git-branch-<%= branch_name %>
    params: {depth: 20}
    trigger: true
  - task: git-branch-task
    file: git-branch/concourse.yml
    config:
      params:
        BRANCH_NAME: <%= branch_name %>
